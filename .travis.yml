language: bash

services:
  - docker

env:
  - ARCH=x86
  - ARCH=arm

script:
  - bash docker/build.sh

deploy:
  provider: releases
  api_key: $GH_TOKEN
  file_glob: true
  file: build/*.tar.gz
  skip_cleanup: true
  on:
    repo: tcoupin/rok4
    tags: true
